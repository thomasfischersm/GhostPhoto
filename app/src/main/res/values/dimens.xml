<resources>
    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">16dp</dimen>
    <dimen name="activity_vertical_margin">16dp</dimen>

    <dimen name="selectedText">16sp</dimen>
    <dimen name="unselectedText">14sp</dimen>

    <!-- Set minimum width for photos in the photo shoot activity. -->
    <dimen name="cover_photo_min_column_width">150dp</dimen>

    <!-- Sets the spacing between items in the photo shoot activity. -->
    <dimen name="photo_shoot_spacing">5dp</dimen>

    <!-- Sets the diameter for round buttons. -->
    <dimen name="round_button_diameter">52dp</dimen>

    <!-- Sets all photo pane row count. -->
    <integer name="all_photo_pane_row_count">3</integer>

    <dimen name="status_bar_height">24dp</dimen>

    <!-- The space between the time interval circles. -->
    <dimen name="button_spacing">16dp</dimen>
</resources>
